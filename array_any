> php/Array

                                   array_any

(PHP 8 >= 8.4.0)

array_any — Checks if at least one [1]array element satisfies a callback
function

  Description

`array_any(array $array, callable $callback): bool`

array_any() returns [2]true, if the given `callback` returns [3]true for any
element. Otherwise the function returns [4]false.

  Parameters

`array`
        The [5]array that should be searched.

`callback`

        The callback function to call to check each element, which must be

 `callback(mixed $value, mixed $key): bool`

        If this function returns [6]true, [7]true is returned from array_any()
        and the callback will not be called for further elements.

  Return Values

The function returns [8]true, if there is at least one element for which
`callback` returns [9]true. Otherwise the function returns [10]false.

  Examples

Example #1 array_any() example

```php
<?php
$array = [
    'a' => 'dog',
    'b' => 'cat',
    'c' => 'cow',
    'd' => 'duck',
    'e' => 'goose',
    'f' => 'elephant'
];

// Check, if any animal name is longer than 5 letters.
var_dump(array_any($array, function (string $value) {
    return strlen($value) > 5;
}));

// Check, if any animal name is shorter than 3 letters.
var_dump(array_any($array, function (string $value) {
    return strlen($value) < 3;
}));

// Check, if any array key is not a string.
var_dump(array_any($array, function (string $value, $key) {
   return !is_string($key);
}));
?>
```

The above example will output:

```
bool(true)
bool(false)
bool(false)
```

  See Also

  • [11]array_all() - Checks if all array elements satisfy a callback function
  • [12]array_filter() - Filters elements of an array using a callback function
  • [13]array_find() - Returns the first element satisfying a callback function
  • [14]array_find_key() - Returns the key of the first element satisfying a
    callback function

© 1997–2025 The PHP Documentation Group
Licensed under the Creative Commons Attribution License v3.0 or later.
[15]https://www.php.net/manual/en/function.array-any.php

References

   Visible links
   1. local://php/Arrays#language.types.array
   2. https://www.php.net/manual/en/reserved.constants.php#constant.true
   3. https://www.php.net/manual/en/reserved.constants.php#constant.true
   4. https://www.php.net/manual/en/reserved.constants.php#constant.false
   5. local://php/Arrays#language.types.array
   6. https://www.php.net/manual/en/reserved.constants.php#constant.true
   7. https://www.php.net/manual/en/reserved.constants.php#constant.true
   8. https://www.php.net/manual/en/reserved.constants.php#constant.true
   9. https://www.php.net/manual/en/reserved.constants.php#constant.true
  10. https://www.php.net/manual/en/reserved.constants.php#constant.false
  11. local://php/array_all#function.array-all
  12. local://php/array_filter#function.array-filter
  13. local://php/array_find#function.array-find
  14. local://php/array_find_key#function.array-find-key
  15. https://www.php.net/manual/en/function.array-any.php
